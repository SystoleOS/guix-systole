From 2d4d1242424f63c6da00919523eae8b63930191a Mon Sep 17 00:00:00 2001
From: Rafael Palomar <rafael.palomar@ous-research.no>
Date: Sun, 22 Feb 2026 18:32:59 +0100
Subject: [PATCH] ENH: Add standalone build support for ExtensionWizard
 scripted module

---
 .../Scripted/ExtensionWizard/CMakeLists.txt   | 29 +++++++++++++++++++
 1 file changed, 29 insertions(+)

diff --git a/Modules/Scripted/ExtensionWizard/CMakeLists.txt b/Modules/Scripted/ExtensionWizard/CMakeLists.txt
index 692cefd64d..cdc3c8d149 100644
--- a/Modules/Scripted/ExtensionWizard/CMakeLists.txt
+++ b/Modules/Scripted/ExtensionWizard/CMakeLists.txt
@@ -1,3 +1,22 @@
+cmake_minimum_required(VERSION 3.16.3...3.19.7 FATAL_ERROR)
+project(ExtensionWizard)
+
+set(CMAKE_CXX_STANDARD "17")
+
+find_package(Slicer REQUIRED)
+include(${Slicer_USE_FILE})
+
+# Slicer_QTSCRIPTEDMODULES_LIB_DIR and Slicer_INSTALL_QTSCRIPTEDMODULES_LIB_DIR
+# are only populated when Slicer is built with Slicer_BUILD_QTSCRIPTEDMODULES=ON.
+if(NOT Slicer_QTSCRIPTEDMODULES_LIB_DIR)
+  set(Slicer_QTSCRIPTEDMODULES_LIB_DIR
+      "lib/Slicer-${Slicer_VERSION}/qt-scripted-modules")
+endif()
+if(NOT Slicer_INSTALL_QTSCRIPTEDMODULES_LIB_DIR)
+  set(Slicer_INSTALL_QTSCRIPTEDMODULES_LIB_DIR
+      "lib/Slicer-${Slicer_VERSION}/qt-scripted-modules")
+endif()
+
 #-----------------------------------------------------------------------------
 set(MODULE_NAME ExtensionWizard)
 
@@ -25,3 +44,13 @@ slicerMacroBuildScriptedModule(
   SCRIPTS ${MODULE_PYTHON_SCRIPTS} ${MODULE_PYTHON_RESOURCES}
   WITH_GENERIC_TESTS
   )
+
+#-----------------------------------------------------------------------------
+# Install the SlicerWizard Python package alongside the module scripts.
+# In a standalone build CMAKE_SOURCE_DIR is Modules/Scripted/ExtensionWizard,
+# so SlicerWizard lives three levels up at Utilities/Scripts/SlicerWizard.
+# Slicer adds qt-scripted-modules/ to sys.path when loading modules from
+# an additional-module-path, so SlicerWizard/ here is importable as a package.
+install(DIRECTORY "${CMAKE_SOURCE_DIR}/../../../Utilities/Scripts/SlicerWizard"
+        DESTINATION "${Slicer_INSTALL_QTSCRIPTEDMODULES_LIB_DIR}"
+        FILES_MATCHING PATTERN "*.py")
-- 
2.52.0

